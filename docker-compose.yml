version: "3.9"

services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - DATABASE_URL=sqlite:////app/db/predictions.db
    volumes:
      - db-data:/app/db
      - ./models:/app/models
    restart: unless-stopped

volumes:
  db-data:
